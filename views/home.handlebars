{{> global/header}}


<div class="container">
	<div class="row">
	  <div class="l-col">
	  	 {{> main/box}}
	  	 {{> main/box}}
	  	 {{> main/box}}
	  </div>
	  <div class="r-col">
	  	{{!-- {{if login=true}}
	  		{{> global/createforum}}
	  	{{else}}

	  	{{/if}} --}}
	  	{{> global/createform}}
	    {{> global/user}}
	    {{> global/timedate}}
	    {{> global/sentiment}}
	  </div>
	</div>  
</div>
	