{{> global/header}}


<div class="container">
	<div class="row">
	  <div class="l-col">
	  	 {{> post/comment}}
	  	 {{> post/comment}}
	  	 {{> post/comment}}
	  	 {{> post/comment}}
	  </div>

	  <div class="r-col">
	  	{{!-- {{if login=true}}
	  		{{> global/createfourm}}
	  	{{else}}

	  	{{/if}} --}}
	    {{> global/user}}
	    {{> global/timedate}}
	    {{> global/sentiment}}
	  </div>
	</div>  
</div>
	